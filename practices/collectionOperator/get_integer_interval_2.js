'use strict';

function get_integer_interval_2(number_a, number_b) {
  let returnArray = [];

  for(; number_a <= number_b ; number_a++){
    if (number_a % 2 === 0) return
  }
}

module.exports = get_integer_interval_2;
