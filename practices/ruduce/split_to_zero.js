'use strict';

function spilt_to_zero(number, interval) {
  let arr = [];

    for (; number > 0; number - interval){
      arr.push(number);
    }
    return returnArr;
}

module.exports = spilt_to_zero;
